<%= form_tag(admin_subscriptions_path, method: :get, class: 'row g-3 mb-3') do %>
  <%= render_filter_text(:stripe_id, 'Stripe ID', col: 3) %>

  <%= render_filter_enum(Subscription.stripe_statuses, :stripe_status, size: 1) %>

  <%= render_filter_submit %>
<% end %>
