<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">LandGrab</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% [
          ['Plots', plots_path],
          ['Blocks', blocks_path]
        ].each do |text, href| %>
          <li class="nav-item">
            <%= active_navlink text, href %>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav mb-2 mb-lg-0">
        <li class="nav-item">
          <% if user_signed_in? %>
            <%= active_navlink 'Log Out', destroy_user_session_path %>
            <% else %>
            <%= active_navlink 'Log In', new_user_session_path %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= active_navlink('GitHub', 'https://github.com/olliebennett/landgrab/') %>
        </li>
      </ul>
    </div>
  </div>
</nav>
