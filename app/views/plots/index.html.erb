<h1><%= title 'Plots' %></h1>

<p>
  Plots are collections of "tiles" (3 by 3 metres in size) covering an area.
</p>

<%= page_entries_info @plots %>

<%= render 'table', plots: @plots %>

<%= paginate @plots, theme: 'bootstrap-5' %>
