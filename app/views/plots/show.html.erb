<h1>
  Plot: <%= title @plot.title %>
</h1>

<div class="row">
  <div class="col-md-6 mb-3">
    <%= render 'details', plot: @plot %>
    <p>
      Select a tile from the map to subscribe or view details.
    </p>
    <%= render 'posts', plot: @plot %>
  </div>
  <div class="col-md-6">
    <%= render 'static_pages/w3w_map', tiles: @plot.tiles_for_map %>
  </div>
</div>
