<h1><%= title 'Subscriptions' %></h1>

<p>
  You are currently subscribed to
  <%= pluralize(@subscriptions.size, 'block') %>
</p>

<%= render 'table', subscriptions: @subscriptions %>

<p>
  You can subscribe to a new (unclaimed) block from any project, plot or block's details page.
</p>
