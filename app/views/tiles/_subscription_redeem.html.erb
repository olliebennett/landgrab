<%= form_with url: redeem_subscription_path(subscription) do |form| %>
  <%= form.hidden_field :tile, value: tile.hashid %>
  <%= form.submit 'Redeem this tile', class: 'btn btn-primary' %>
  using your existing subscription.
<% end %>
